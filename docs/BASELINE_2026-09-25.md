# Baseline before the redesign (measured 2026-09-25)

Live site eiger014.com at main 7f38191, Lighthouse 12 via npx, headless Chrome, default throttling.

| | Desktop preset | Mobile (default) |
|---|---|---|
| Performance | 99 | 90 |
| Accessibility | 96 | 96 |
| LCP | 0.8 s | 3.3 s |
| TBT | 0 ms | 180 ms |
| CLS | 0.017 | 0 |
| Total bytes | 10,920 KiB | 6,926 KiB |

Build at 7f38191 (`npm run build`): see scratch `baseline/build-7f38191.txt` for chunk sizes; main JS chunk 445.46 kB (131.60 kB gzip).

Acceptance targets for the redesign (Codex: targets, not results): desktop 60 fps with no long task > 50 ms during scroll on a 2020-class laptop; phone Lighthouse performance >= 80 with desktop-only motion disabled; total added motion JS <= 80 KB gzip; re-measure with the same command and record here.
